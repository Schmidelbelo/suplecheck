import { MarketplaceProviderStubBase } from "./MarketplaceProviderStubBase";

/** Stub — futura integração via Shopee Open Platform API (exige aprovação de parceiro). */
export class ShopeeMarketplaceProviderStub extends MarketplaceProviderStubBase {
  readonly marketplaceName = "Shopee";
}
