{
  "name": "@suplecheck/infrastructure",
  "version": "0.1.0",
  "private": true,
  "description": "Infrastructure Layer do SupleScore — implementa os Ports da Application Layer (persistência, cache, storage, mail, filas, APIs externas...). Depende de @suplecheck/application; nunca o contrário.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@suplecheck/application": "0.1.0",
    "@suplecheck/core": "0.1.0",
    "@prisma/client": "*"
  }
}
