{
  "name": "@suplecheck/core",
  "version": "0.1.0",
  "private": true,
  "description": "Core Domain do SupleScore — motor de cálculo do Índice SupleScore. Zero dependências de framework.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {}
}
