{
  "name": "@suplecheck/application",
  "version": "0.1.0",
  "private": true,
  "description": "Application Layer do SupleCheck — orquestra o Core Domain via Use Cases, Ports e DTOs. Depende de @suplecheck/core; nunca o contrário.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@suplecheck/core": "0.1.0"
  }
}
